import { BigNumber } from "@ethersproject/bignumber";
import { Provider } from "@ethersproject/providers";
declare const useBalance: (provider: Provider, address: string, pollTime?: number) => BigNumber;
export default useBalance;
