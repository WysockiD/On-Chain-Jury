import { Emitter, BitcoinTransactionLog } from './interfaces';
declare function transaction(this: any, hash: string, id?: string): {
    details: BitcoinTransactionLog;
    emitter: Emitter;
};
export default transaction;
